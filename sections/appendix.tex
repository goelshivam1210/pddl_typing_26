\clearpage
\onecolumn
\section{Appendices}

\subsection{Consensus Sample-Count Ablation}
\label{app:consensus}

The main results report consensus at $n{=}3$. Here we ablate the number of samples
$n\in\{1,3,5\}$, where $n{=}1$ issues a single query with no voting or relaxation and
therefore isolates the model's single-shot accuracy. Table~\ref{tab:consensus_recovery}
reports recovery and Table~\ref{tab:consensus_refusal} refusal.

\paragraph{Recovery.} Recovery generally improves with $n$ but with diminishing returns:
on \textsc{single} and \textsc{stress} the Gemma4 variants are already saturated at
$n{=}1$, and the largest gains appear on \textsc{bulk} and for \textsc{Deepseek}, whose
single-shot accuracy is lower. Because token cost scales roughly linearly with $n$
(about $3.7$k, $11$k, and $18$k tokens per problem at $n{=}1,3,5$), $n{=}3$ is a
reasonable default and $n{=}5$ rarely justifies its cost.

% ========== CONSENSUS RECOVERY ABLATION ==========
\begin{table}[ht]
\centering
\caption{Consensus recovery (\%) by sample count $n\in\{1,3,5\}$ (rows grouped by model). Recovery generally improves with $n$ but with diminishing returns, while token cost scales roughly linearly with $n$.}
\label{tab:consensus_recovery}
\footnotesize
\renewcommand{\arraystretch}{0.9}
\resizebox{\textwidth}{!}{%
\begin{tabular}{l|ccccc|cccc}
\toprule
 & \multicolumn{5}{c|}{\textbf{MacGyver Categories}} & \multicolumn{4}{c}{\textbf{Synonym Domains (single)}} \\
\midrule
\textbf{Config} & \textbf{single} & \textbf{bulk} & \textbf{stress-10} & \textbf{stress-20} & \textbf{stress-50} & \textbf{Electronics} & \textbf{Kitchen} & \textbf{Medic} & \textbf{Survival} \\
\midrule
DS-n1       & 86.0 $\pm$ 13.5 & 42.0 $\pm$ 39.4 & 78.0 $\pm$ 31.9 & 86.0 $\pm$ 21.2 & 56.0 $\pm$ 29.5 & 86.0 $\pm$ 13.5 & 70.0 $\pm$ 28.7 & 98.0 $\pm$ 6.3 & 90.0 $\pm$ 17.0 \\
DS-n3       & \textbf{100.0} $\pm$ 0.0 & 54.0 $\pm$ 38.9 & 86.0 $\pm$ 29.9 & 82.0 $\pm$ 29.0 & 88.0 $\pm$ 21.5 & 98.0 $\pm$ 6.3 & 88.0 $\pm$ 16.9 & \textbf{100.0} $\pm$ 0.0 & 96.0 $\pm$ 12.6 \\
DS-n5       & \textbf{100.0} $\pm$ 0.0 & 58.0 $\pm$ 50.3 & 90.0 $\pm$ 25.4 & 82.0 $\pm$ 29.0 & 86.0 $\pm$ 21.2 & \textbf{100.0} $\pm$ 0.0 & 92.0 $\pm$ 14.0 & 98.0 $\pm$ 6.3 & \textbf{100.0} $\pm$ 0.0 \\
\midrule
G4-n1       & \textbf{100.0} $\pm$ 0.0 & 60.0 $\pm$ 51.6 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & 98.0 $\pm$ 6.3 & \textbf{100.0} $\pm$ 0.0 & 98.0 $\pm$ 6.3 \\
G4-n3       & \textbf{100.0} $\pm$ 0.0 & 60.0 $\pm$ 51.6 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & 98.0 $\pm$ 6.3 \\
G4-n5       & \textbf{100.0} $\pm$ 0.0 & 60.0 $\pm$ 51.6 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 \\
\midrule
G4NT-n1     & \textbf{100.0} $\pm$ 0.0 & \textbf{64.0} $\pm$ 39.8 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & 98.0 $\pm$ 6.3 & \textbf{100.0} $\pm$ 0.0 & 98.0 $\pm$ 6.3 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 \\
G4NT-n3     & \textbf{100.0} $\pm$ 0.0 & 58.0 $\pm$ 50.3 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 \\
G4NT-n5     & \textbf{100.0} $\pm$ 0.0 & \textbf{64.0} $\pm$ 47.9 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 & \textbf{100.0} $\pm$ 0.0 \\
\bottomrule
\end{tabular}}
\end{table}

\paragraph{Refusal.} Refusal moves in the opposite direction --- it \emph{degrades} as
$n$ grows. This is a direct consequence of the consensus vote-rank relaxation, which on
planner failure re-types low-confidence objects to any alternative that received a vote,
searching for a solvable assignment. On an unsolvable problem this search is exactly what
should be avoided, and it worsens with the sample budget: more samples accumulate more
distractor$\rightarrow$tool proposals, so relaxation is more likely to find one the
planner accepts. \textsc{Deepseek} at $n{=}5$ is the extreme case, collapsing to $16\%$
from $64\%$ and $60\%$ at $n{=}1$ and $n{=}3$; of its $42$ false positives, $28$ were
produced by relaxation retyping a distractor such as \texttt{ripe-banana} to
\texttt{hammer}. The mechanism that aids recovery thus directly undermines refusal, which
is why the main results adopt the more conservative $n{=}3$.

% ========== CONSENSUS REFUSAL ABLATION ==========
\begin{table}[ht]
\centering
\caption{Consensus refusal accuracy on \textsc{no\_sub} problems by sample count. Refusal \emph{degrades} as $n$ grows: vote-rank relaxation searches harder for a solvable assignment and, on an unsolvable problem, is more likely to accept a distractor. \textsc{Deepseek} collapses at $n{=}5$.}
\label{tab:consensus_refusal}
\footnotesize
\renewcommand{\arraystretch}{0.9}
\begin{tabular}{l|ccc|c}
\toprule
\textbf{Method} & \textbf{DS} & \textbf{G4} & \textbf{G4NT} & \textbf{Avg} \\
\midrule
Cons n=1   & \textbf{64.0} $\pm$ 36.3 & \textbf{66.0} $\pm$ 47.2 & \textbf{52.0} $\pm$ 48.3 & \textbf{60.7} $\pm$ 7.6 \\
Cons n=3   & 60.0 $\pm$ 32.7 & 56.0 $\pm$ 46.0 & 50.0 $\pm$ 46.4 & 55.3 $\pm$ 5.0 \\
Cons n=5   & 16.0 $\pm$ 18.4 & 62.0 $\pm$ 44.7 & 48.0 $\pm$ 48.3 & 42.0 $\pm$ 23.6 \\
\bottomrule
\end{tabular}
\end{table}

% Generated by examples/export_prompts_tex.py
\renewcommand{\lstlistingname}{Prompt}
\subsection{Prompts Used for Experimentation}
\label{app:prompts}
All domains use the constrained typing pipeline: the domain actions already enforce specific subtypes, so the LLM only reclassifies problem objects into those existing subtypes. Every mode shares the same system prompt and two-step typing pipeline (Prompt~\ref{lst:typing-prompt}); the judge mode additionally issues the evaluation prompt (Prompt~\ref{lst:judge-prompt}) after each typing query. The consensus modes issue the typing prompt $n$ independent times and aggregate by majority vote. In the iterative and judge modes the \texttt{\{\{JUDGE\_FEEDBACK\}\}} placeholder is populated with planner or judge feedback and the typing prompt is re-issued; it is empty on the first pass. \texttt{\{\{PLANNING\_PROBLEM\}\}} and \texttt{\{\{TYPE\_CLASSIFICATION\}\}} are substituted with the natural-language task encoding and the proposed classification, respectively.

\begin{lstlisting}[caption={Typing Prompt (two-step reasoning pipeline)}, label={lst:typing-prompt}, breaklines=true, numbers=none, basicstyle=\footnotesize\ttfamily, columns=flexible, frame=single, framesep=4pt, literate={"}{{\char34}}{1}]
[SYSTEM]
You are an expert in functional object classification for automated planning. Given a planning problem, identify which available objects can plausibly substitute for each specific functional role required by domain actions, based on their physical properties and typical use. If no available object is a plausible match for a required role, leave all objects at their base type. Think concisely -- follow the steps directly and reach your conclusion without revisiting decisions already made.
------------------------------------------------------------------------
[HUMAN] Step: analyze_required_type
# PLANNING PROBLEM

{{PLANNING_PROBLEM}}

# TASK: IDENTIFY REQUIRED FUNCTIONAL ROLES

Domain actions already enforce specific subtypes (visible in DOMAIN ACTIONS). Your job is to identify which available objects can plausibly fill those roles.

## Step 1 -- Goal and direct action
What is the goal? Which action directly achieves it, and what subtype(s) does it require?

## Step 2 -- Precondition chain
Check the PRE line of the goal action against the INITIAL STATE.
IMPORTANT: If INITIAL STATE shows '(No initial facts specified)', then NO predicates hold initially -- every precondition is MISSING and must be produced by a prior action.
- For each missing precondition: find which action's EFF produces it. What subtype does that action require?
- Repeat until every prerequisite is satisfied.
End this step with a numbered list: "Required roles: 1) [type], 2) [type], ..."

## Step 3 -- Object matching
Work through your required roles list one by one. For EACH role:
1. State the required physical action in concrete terms (e.g. 'deliver heavy blunt impact', 'fit a narrow slot and apply rotational torque').
2. For EACH available object, briefly describe its physical form (e.g. heavy/light, rigid/soft, shape). Then ask: could someone physically perform the required action using an object with these properties, even clumsily or unconventionally? Focus entirely on physical form -- ignore what the object is called or intended for.
Name the best candidate, or state "No match" only if truly no object's physical form could accomplish the action.
Do not skip any role from Step 2.

## Step 4 -- Distractors
List objects that cannot plausibly serve any required role.

{{JUDGE_FEEDBACK}}

## Step 5 -- Final match summary
List ONLY the confirmed matches from Step 3 as:
  [exact_object_name] -> [required_role_subtype]
The subtype is the ROLE it fills (e.g. hammer, wrench, screwdriver), NOT the object's original type. One line per match. If no matches, write: No matches.

State your findings for each required role now.
------------------------------------------------------------------------
[AI]
<intermediate response>
------------------------------------------------------------------------
[HUMAN] Step: generate_classification
Based on your analysis, provide the structured classification:

**types**: List only the specific subtype(s) you are actually assigning to at least one object.
CRITICAL: Use ONLY type names from DOMAIN TYPE HIERARCHY. Do not invent new types.
If no object plausibly fits any required role, set types to [].

**object_types**: Classify EVERY object using its exact object name as the key.
Example: {"object_a": "hammer", "object_b": "tool"}
Do NOT use type names or category headings as keys -- use the exact object name.
- Objects you identified as plausible matches above -> the required specific subtype
- ALL other objects -> their original or closest base type from DOMAIN TYPE HIERARCHY
IMPORTANT: Only assign a specific subtype to an object if your analysis above identified it as a match. If you concluded 'No match' for a role, do not assign any object to that subtype.
CRITICAL: Every type value must be a name from DOMAIN TYPE HIERARCHY.

Provide your complete structured output now.
\end{lstlisting}

\begin{lstlisting}[caption={Judge Prompt (self-critique in judge mode)}, label={lst:judge-prompt}, breaklines=true, numbers=none, basicstyle=\footnotesize\ttfamily, columns=flexible, frame=single, framesep=4pt, literate={"}{{\char34}}{1}]
[SYSTEM]
You are an expert in functional object classification for automated planning. Given a planning problem, identify which available objects can plausibly substitute for each specific functional role required by domain actions, based on their physical properties and typical use. If no available object is a plausible match for a required role, leave all objects at their base type. Think concisely -- follow the steps directly and reach your conclusion without revisiting decisions already made.
------------------------------------------------------------------------
[HUMAN] Step: evaluate_classification
# PLANNING PROBLEM

{{PLANNING_PROBLEM}}

# PROPOSED TYPE CLASSIFICATION

{{TYPE_CLASSIFICATION}}

# YOUR TASK: EVALUATE THIS CLASSIFICATION

You are an expert judge evaluating whether this type classification is good.

## Evaluation Criteria

1. **Type Validity**: Are all assigned types from DOMAIN TYPE HIERARCHY?
   - Check every value in object_types and every entry in types.
   - Flag any type name that does not appear in DOMAIN TYPE HIERARCHY as invalid.
   - Invalid types will be discarded by the planner -- this may break solvability.

2. **Goal Alignment**: Do the types match what the goal needs?
   - Are goal-relevant objects reclassified to the correct specific subtype?
   - Are distractor objects left at their original or closest base type?

3. **Solvability**: Will this typing let the planner solve the problem?
   - Is the key object reclassified to the exact subtype the relevant action requires?
   - Can the goal still be achieved with these type assignments?

## Your Decision

Provide:
- **is_approved**: true if good, false if needs refinement
- **solvability_assessment**: 'likely_solvable', 'uncertain', or 'likely_unsolvable'
- **feedback**: Detailed feedback on issues (if rejecting) or strengths (if approving)
- **confidence**: 0.0 to 1.0 (how confident are you?)

If you reject, explain specifically what needs to change.
If you approve, explain why the classification is good.

Provide your structured judgment now.
\end{lstlisting}
